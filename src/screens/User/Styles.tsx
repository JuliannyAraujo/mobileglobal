import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    container: {
        backgroundColor: '#131519',
        minHeight: '100vh',
        alignItems: 'center',
        justifyContent: 'flex-start',
    },
    textTitle: {
        fontSize: 36,
        color: 'blue', // Alterado para azul
        borderBottomWidth: 1,
        paddingVertical: 8,
        borderBlockColor: '#9CE5C9',
        width: '100vw',
        marginBottom: 8,
        textAlign: 'center'
    }
});

export default Styles;
